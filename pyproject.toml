[project]
name = "mfpi"
version = "0.1.0"
requires-python = ">=3.12"
description = "Auditable Mississippi high-school football computer rankings"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
